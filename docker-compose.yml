version: '3.9'

services:
  backend:
    container_name: ${BACKEND_HOSTNAME}
    ports: 
      - "${BACKEND_PORT}:8080"
    build:
      context: ./packages/server
      dockerfile: Dockerfile
    environment:
      PORT: 8080
    env_file:
      - .env
      - .env.local